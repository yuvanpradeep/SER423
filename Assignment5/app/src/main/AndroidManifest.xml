<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.asu.msse.yparamas.assignment5">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        tools:ignore="GoogleAppIndexingWarning"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name="edu.asu.msse.yparamas.assignment5.DetailView"></activity>
        <activity android:name="edu.asu.msse.yparamas.assignment5.CircleDistanceBearing"></activity>
        <activity android:name="edu.asu.msse.yparamas.assignment5.AddPlace"></activity>

        <activity android:name="edu.asu.msse.yparamas.assignment5.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>